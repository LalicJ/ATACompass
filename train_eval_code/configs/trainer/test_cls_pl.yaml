_target_: pytorch_lightning.Trainer

devices: "1"
# devices: 0
accelerator: gpu
log_every_n_steps: 10
limit_val_batches: 1.0     # validate on full dataset, can be used to toggle quick run

logger:
  _target_: pytorch_lightning.loggers.WandbLogger
  project: base_seqs_cls
  name: ~
  save_dir: ~