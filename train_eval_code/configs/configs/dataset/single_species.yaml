_name_: single_species
bed_file: /data/home/ia00/hyena-dna/data/20231109_scATAC/regions_cerebellum_split.bed
fasta_file: /data/home/ia00/hyena-dna/data/20231109_scATAC/regions_cerebellum.fa
species: [human, mouse, hippo, pig, lemur]
test_split: val
dataset_name: hg38
tokenizer_name: char
cache_dir: null
max_length: 1024
add_eos: True
batch_size: 1  # per GPU
batch_size_eval: ${eval:${.batch_size} * 2}
num_workers: 4  # For preprocessing only
shuffle: True
pin_memory: True
target: 0
__train_len: ${div_up:1_000_000_000, ${.max_length}}
__l_max: ${.max_length}